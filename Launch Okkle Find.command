#!/usr/bin/env bash
# Double-click this file in Finder to launch the Okkle Find dev server.
# (macOS opens it in Terminal.) It then opens your browser automatically.
cd "$(dirname "$0")"
exec bin/launch
